# UltiMenu — UAT Checklist

This document is the executable companion to `FEATURES.md`: one row per feature stating the steps
to exercise it and the observable truth that proves it works. It is an internal reference for
real-machine verification, not user-facing documentation.

> Batches are dispatched at 60 rows or fewer, and a batch never spans two repositories. There are
> exactly two legitimate exits to `human-uat-pending`: a row needing the pixel layer while the
> real-client harness is not ready, and a row needing personal credentials. Every other row must
> reach `pass`, `fail`, or `blocked`.

## Conventions

- **Columns:** `ID`, `Preconditions`, `Steps`, `Expected`, `Layer`, `Covers`.
- **ID:** cites its `FEATURES.md` ID verbatim. A negative case suffixes the checklist ID only, as
  `.neg-<slug>` — a negative case still tests the same feature, so the base ID is unchanged.
- **Layer**, copied verbatim from the real-client acceptance tooling's fixed vocabulary so no translation
  step exists at dispatch time: `protocol`, `java-client`, `os-input`, `pixel`, `server`, `human`.
  **This module's own division of labor across those values:** a row asserting that
  specific items, in specific slots, with specific names/lore/colors, appear inside the rendered
  GUI is `pixel` — nothing short of a real client window and a validated screenshot settles
  "does the menu look right", and this module's whole product is that GUI. A row asserting that a
  GUI *of some title* opened at all (without asserting its contents) is `java-client` — a
  programmatic client observing the inventory-open packet's title can settle that without a
  screenshot. A row asserting a server-side side effect of a click (an economy withdrawal, a
  dispatched command, a chat/console message) is `server`. No row in this document is downgraded
  from `pixel` to `protocol` to make it easier to run — an unready pixel harness is a legitimate
  `human-uat-pending` exit; a `protocol` assertion standing in for a rendering claim is not.
- **Human-authenticated-session rows:** none exist in this module — it has no capability
  gated behind the maintainer's own UltiCloud panel session or an SMTP-gated recovery flow. Stated
  here for template consistency with the framework's own checklist.
- **Covers** back-references a GUI class excluded from the JaCoCo coverage gate; left blank when no such class
  applies. This module's excluded class is `CustomMenuGui` — every row below whose `Source` cites
  that class carries it in `Covers`.
- A row whose Preconditions name a prior row must appear after that row in file order — asserted
  mechanically: for every row, every checklist ID cited in its Preconditions cell must have a
  strictly smaller line number in this file than the row citing it.
- **The shipped `example` menu is obtained, not assumed.** A row whose Preconditions name the shipped
  `example` menu, or `menus/example.yml` at its shipped content, means the file THIS build writes. Obtain
  it by deleting the module's whole `menus/` folder (`plugins/UltiTools/pluginConfig/UltiTools-Menu/menus/`)
  and then running `/menu reload` from the console (or restarting the server):
  `MenuServiceImpl#loadMenus` copies the example only when that folder does not exist, so deleting just
  `example.yml` regenerates nothing (if it was the only file, the reload then loads no menu at all), and installing a new jar over
  an existing install never replaces the file already on disk. A server first started on a pre-6.3.0
  build therefore still carries that build's `example.yml`, which differs from this build's in exactly
  one line, a top-level `command: servermenu` (`UltiKits/UltiMenu#12`). Only
  `ultimenu.config.menu-definition-yml` and its `.neg-removed-command` row read that line; every other
  row gives the same verdict on the old file, but runs from the regenerated one anyway, so that every
  row starts from one fixture.
- **Config-per-file rule:** one checklist row per `@ConfigEntity`-annotated class or per
  shipped-format yml file, never one row per key. Two such rows exist here: `ultimenu.config.
  config-yml` (the one `@ConfigEntity` class) and `ultimenu.config.menu-definition-yml` (the
  runtime-parsed menu-definition format, exercised through the shipped `menus/example.yml`).
- This module's shipped defaults are `language: "zh"` (core `config.yml`, not this module's own).
  Every chat line, the command description and every console line this module writes comes from its
  language catalogue (`lang/en.json`, `lang/zh.json`, ASCII keys). Every row below whose Expected
  quotes a literal in-game or console line therefore carries the precondition `language: en` set in
  `plugins/UltiTools/config.yml`, so the observed line matches this document's English text exactly.
  An install upgraded from an earlier build keeps its already-extracted `lang/` files
  (`UltiKits/UltiTools-Reborn#459`); the renamed keys are absent from those files and render from the
  jar, so the quoted text holds either way, but a session moves
  `plugins/UltiTools/pluginConfig/UltiTools-Menu/lang/` aside before the start so the files are
  extracted fresh (`ultimenu.i18n.language`).

## Menu Commands

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultimenu.menu.list | `language: en`; sender holds `ultikits.menu.use`; at least one menu loaded (the shipped `menus/example.yml`, unmodified (obtained per Conventions, "The shipped `example` menu is obtained, not assumed")) | Run `/menu list` | Chat/console shows `=== Available Menus ===` (gold), then one line per loaded menu reading `<filename> - <color-translated title>` (aqua filename, white separator) — for the shipped default: `example - Server Menu - {player}`, with the LITERAL, UNRESOLVED `{player}` token still in the string. `MenuCommands#onList` only runs `ChatColor.translateAlternateColorCodes` on the raw title; `{player}`/PlaceholderAPI substitution happens only in `CustomMenuGui`'s constructor when a menu is actually opened (see `ultimenu.menu.render`), never in the list output | server | |
| ultimenu.menu.list.neg-empty | `language: en`; sender holds `ultikits.menu.use`; the `menus/` folder exists but contains zero `.yml` files (rename or remove `example.yml`, THEN run `/menu reload` AS a sender who ALSO holds `ultikits.menu.admin` — `MenuCommands#onReload` requires that node separately, and without it the reload is refused, `example.yml`'s in-memory menu stays loaded, and this row's own setup silently fails to take effect) | Run `/menu list` | Chat/console shows `No menus available` (yellow) — no header, no entry lines; `MenuServiceImpl#loadMenus` logged `No menu configuration files found` on the preceding reload | server | |
| ultimenu.menu.open | `language: en`; the shipped `example` menu present (obtained per Conventions, "The shipped `example` menu is obtained, not assumed"); sender is a player holding `ultikits.menu.use` | Run `/menu open example` | A GUI opens titled `Server Menu - <player>` (color-translated, `{player}` resolved to the opening player's own name) — see `ultimenu.menu.render` for the full slot-by-slot content assertion | java-client | |
| ultimenu.menu.open.neg-console | `language: en`; run from console (not a player) | Run `/menu open example` | The command is rejected before `MenuCommands#onOpen` runs at all — the framework's own sender-type validator refusal (this mapping carries `@CmdTarget(PLAYER)`), not this module's own hand-written message; no GUI opens, nothing else happens | server | |
| ultimenu.menu.quick-open | `language: en`; the shipped `example` menu present (obtained per Conventions, "The shipped `example` menu is obtained, not assumed"); sender is a player holding `ultikits.menu.use` (the class-level `@CmdExecutor` permission gates `/menu <name>` exactly as it gates `/menu open <name>`) | Run `/menu example` | A GUI opens titled `Server Menu - <player>`, functionally identical to `ultimenu.menu.open`'s result | java-client | |
| ultimenu.menu.quick-open.neg-console | `language: en`; run from console | Run `/menu example` | The console receives exactly one line, `Only players can open menus!` (red), and no menu is opened. Before `UltiKits/UltiMenu#11` was fixed this line was a Chinese sentence under every `language` setting | server | |
| ultimenu.menu.quick-open.neg-not-found | `language: en`; sender is a player holding `ultikits.menu.use`; no menu named `does-not-exist` is loaded | Run `/menu does-not-exist` | Chat shows `Menu 'does-not-exist' does not exist!` (red) | server | |
| ultimenu.menu.reload | `language: en`; sender holds both `ultikits.menu.use` and `ultikits.menu.admin`; ONLY `menus/example.yml` is present in the `menus/` folder (no other `.yml` file — `MenuCommands#onReload` reports `menuService.getAllMenus().size()`, the TOTAL loaded-menu count, not a hardcoded 1; with any additional menu file present the correct count would differ from 1 and this row's own literal expectation would misfire); edit `menus/example.yml`'s `title` to a distinguishable value on disk first (do not reload yet) | Run `/menu reload`, then `/menu list` | Chat/console shows `Reloaded 1 menus` (green, because exactly one menu file is loaded per this row's own precondition); the immediately following `/menu list` shows the EDITED title, not the previous one — `MenuServiceImpl#loadMenus` re-parsed the file from disk | server | |
| ultimenu.menu.reload.neg-permission | `language: en`; sender holds `ultikits.menu.use` (so it reaches `onReload` at all) but NOT `ultikits.menu.admin` | Run `/menu reload` | Chat shows `You don't have permission to execute this command!` (red); `menuService.reload()` is never called — the sender's own edited-on-disk title from a prior attempt (if any) is NOT picked up, confirming no reload happened | server | |

## Item Binding

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultimenu.itembind.trigger | The shipped `example` menu present (obtained per Conventions, "The shipped `example` menu is obtained, not assumed") (`bind-item: COMPASS`, `bind-name: "Server Menu"` [color-translated], `bind-lore: "Right-click to open"` [color-translated]); sender's MAIN hand holds a compass with that exact display name and a lore line containing that substring; sender holds `ultikits.menu.use`, which this path now requires as the command path always did (`UltiKits/UltiMenu#14`) — `example` ships `permission: null`, an explicit YAML null that `MenuServiceImpl#parseMenuFile` reads as absent, so the base node is the only thing gating this row | Right-click (air or a block) while holding the compass | The interaction is cancelled (the compass is not otherwise used); a GUI opens for the `example` menu, functionally identical to `ultimenu.menu.open`'s result | java-client | |
| ultimenu.itembind.trigger.neg-base-permission | `language: en`; exactly the fixture of `ultimenu.itembind.trigger` (same shipped `example` menu, whose `permission` key ships as an explicit YAML `null` and therefore imposes no per-menu node, and the same matching compass in the main hand), EXCEPT the holder does NOT hold `ultikits.menu.use` — set it to false explicitly in the permission plugin rather than merely leaving it ungranted, so the row does not rest on what the server's default permission set happens to contain | Right-click (air or a block) while holding the matching compass | Chat shows `You don't have permission to open this menu!` (red) and NO menu opens. Then run the control, which must not depend on how fast a permission plugin applies a live change: grant `ultikits.menu.use`, **read the grant back with a command that resolves the node for the live player** — `/lp user <player> permission check ultikits.menu.use`, or the installed plugin's equivalent — and only proceed once it answers true, then right-click again — the menu opens. That attributes the refusal to the node rather than to a fixture that stopped matching (`UltiKits/UltiMenu#14`). Without the read-back a plugin that applies changes on next login produces a `failed` verdict against a correct build. The event is also still cancelled, but this fixture cannot show that — a compass has no ordinary right-click effect to suppress — so this row does not assert it; `ItemBindListenerTest$BasePermissionTests#shouldRefuseWithoutBaseNode` asserts the cancellation instead | java-client | |
| ultimenu.itembind.trigger.neg-wrong-name | Same compass item type, but its display name does NOT match `example`'s `bind-name` (a plain, unrenamed compass); the holder DOES hold `ultikits.menu.use`, so a failure to open is attributable to the name mismatch and not to the base node this path now also requires | Right-click while holding the plain compass | No menu opens; the interaction proceeds normally (e.g. the compass points to its lodestone/spawn as usual, no `event.setCancelled` was called for this item) | java-client | |
| ultimenu.itembind.trigger.off-hand | Main hand holds an unrelated item (e.g. dirt); off-hand holds the same matching compass as `ultimenu.itembind.trigger`; the holder holds `ultikits.menu.use` | Right-click while holding both | A GUI opens for the `example` menu — `ItemBindListener#onPlayerInteract` fell through the main-hand check (dirt does not match any menu's `bind-item`) and matched on the off-hand item instead | java-client | |

## GUI Rendering

**Why no row exercises the base node on the sub-menu path.** `MenuAccess#allowOpen` requires
`ultikits.menu.use` for the sub-menu too, but no row below asserts it, because a real server
cannot produce the situation: to click a linking button a player must already be inside the
parent menu, and every way into a menu now requires the same node. Contriving it means revoking
the node from a player while his inventory is open, which would make the row's verdict depend on
how promptly the installed permission plugin applies a live revocation — a platform behaviour
this document would then be resting on without establishing it. That branch is covered instead by
`CustomMenuGuiTest$ButtonSubMenuAccessTests#shouldRefuseWithoutBaseNode`, and it is defence in
depth rather than the gate anyone reaches first.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultimenu.menu.render | The shipped, unmodified `example` menu (obtained per Conventions, "The shipped `example` menu is obtained, not assumed"); sender is a player holding `ultikits.menu.use`; PlaceholderAPI installed with the `server` expansion (for `%server_online%`) | Run `/menu example` and observe the opened inventory | The inventory has exactly 3 rows (27 slots, per `size: 27`), titled `Server Menu - <player>`; slot 10 holds a BOOK named `Server Info` (bold, aqua) with 2 lore lines (`Welcome to the server!` and `Online: <n>`, the second with the live online-player count substituted for `%server_online%`); slot 12 holds an ENDER_PEARL named `Spawn` (bold, pink) with 2 lore lines; slot 14 holds an IRON_SWORD named `Starter Kit` (bold, yellow) with 2 lore lines; slot 16 holds a WRITABLE_BOOK named `Server Rules` (bold, red) with 1 lore line; every other slot is empty | pixel | CustomMenuGui |
| ultimenu.menu.render.neg-empty | A menu definition yml with `buttons:` omitted entirely (no key at all, not merely an empty map) | Open that menu | The inventory opens (correct size and title) but shows NO items in ANY slot — `CustomMenuGui#onOpen` returns immediately when `menuDefinition.getButtons()` is null | pixel | CustomMenuGui |
| ultimenu.menu.button-click | The shipped `example` menu (obtained per Conventions, "The shipped `example` menu is obtained, not assumed") open; sender clicks the `spawn` button (`ENDER_PEARL`, no `permission`, `price: 0`, `player-commands: ["spawn"]`, `close-on-click` unset -> default `true`) | Click the `spawn` button | The inventory closes; the server dispatches `/spawn` AS the clicking player (`Player#performCommand`), synchronously — confirmed by the player's own subsequent teleport/command-feedback for `/spawn` on this server | server | CustomMenuGui |
| ultimenu.menu.button-click.neg-debounce | A test menu (NOT the shipped `example` menu — its only non-navigating, command-carrying button is `spawn`, whose `close-on-click` is unset and therefore defaults to `true`, closing the inventory after the FIRST click and making a second click physically undeliverable regardless of the debounce logic) with a button set to `close-on-click: false` and `player-commands: ["spawn"]`; `click_cooldown_ms` at its shipped default (200ms) | Click that button twice in rapid succession, well under 200ms apart (a scripted double-click, not two manual clicks) | Only the FIRST click's `/spawn` dispatch is observed; the second click, inside the debounce window, produces no server-side effect at all — `CustomMenuGui#handleButtonClick` returns before doing anything, and (because `close-on-click: false`) the inventory is still open to receive that second click in the first place | server | CustomMenuGui |
| ultimenu.menu.button-click.neg-permission | `language: en`; a button in a test menu with `permission: "test.button.gate"` set; the clicking player does NOT hold that node | Click the gated button | Chat shows `You don't have permission to use this button!` (red); none of the button's configured commands run, and the inventory does NOT close | server | CustomMenuGui |
| ultimenu.menu.button-click.economy | `language: en`; the shipped `example` menu (obtained per Conventions, "The shipped `example` menu is obtained, not assumed") open; sender clicks `kit-starter` (`IRON_SWORD`, `price: 100`, 3 console commands); Vault + an Economy provider installed; sender's balance >= 100 | Click the `kit-starter` button | Chat shows `Deducted <F>` (green), where `<F>` is EXACTLY whatever the installed Vault provider's own `Economy#format(100)` returns (`EconomyUtils#format` delegates entirely to the provider — do NOT assume a `$`-prefixed two-decimal string; a different provider may render `100.00 dollars` or another locale-specific form; read the provider's own formatted output before comparing); the sender's balance decreases by exactly 100 (verify via the provider's own balance query, independent of the displayed string's format); on the NEXT server tick, the three console commands run (`give <player> iron_sword 1`, `give <player> iron_pickaxe 1`, `give <player> bread 16`), delivering those items to the player's inventory; the GUI closes (`close-on-click` unset -> default `true`) | server | CustomMenuGui |
| ultimenu.menu.button-click.neg-economy-insufficient | Same as `ultimenu.menu.button-click.economy` (including `language: en`), except the sender's balance is < 100 | Click the `kit-starter` button | Chat shows `Insufficient balance! Requires <F>` (red), where `<F>` is the SAME provider-formatted string described in `ultimenu.menu.button-click.economy` (never assume a `$`-prefixed form); NO balance change, none of the three console commands run, the GUI does NOT close | server | CustomMenuGui |
| ultimenu.menu.button-click.neg-economy-unavailable | `language: en`; same as `ultimenu.menu.button-click.economy`, except Vault (or any Economy provider) is NOT installed | Click the `kit-starter` button | Chat shows `Economy system is not available!` (red); no balance check is even attempted, none of the three console commands run | server | CustomMenuGui |
| ultimenu.menu.submenu-open | The shipped `example` menu (obtained per Conventions, "The shipped `example` menu is obtained, not assumed") open; sender clicks `rules` (`WRITABLE_BOOK`, `open-menu: rules`); a second menu file `menus/rules.yml` is loaded with at least one distinct button of its own and NO `permission` key of its own, and the sender holds `ultikits.menu.use` — the sub-menu being entered is now judged by the same rule as any other menu (`UltiKits/UltiMenu#15`) | Click the `rules` button | The `example` inventory closes; on the NEXT server tick, a NEW inventory opens for the `rules` menu, showing THAT menu's own title and buttons — visually and titularly distinct from `example`'s inventory, not a re-render of the same GUI | pixel | CustomMenuGui |
| ultimenu.menu.submenu-open.neg-not-found | `language: en`; a test menu with a button whose `open-menu` names a menu that is NOT currently loaded (e.g. `does-not-exist`) | Click that button | Chat shows `Menu 'does-not-exist' does not exist!` (red); the CURRENT menu's inventory stays open — `CustomMenuGui#handleButtonClick` returns before calling `player.closeInventory()` | server | CustomMenuGui |
| ultimenu.menu.submenu-open.neg-permission | `language: en`; a test parent menu the clicking player IS allowed to open (leave its `permission` unset, or set one the player holds) with a button whose `open-menu` names a test sub-menu carrying `permission: "test.submenu.gate"`; the player holds `ultikits.menu.use` but NOT `test.submenu.gate`, so the sub-menu's own key is the only thing that can refuse | Open the parent menu, then click the linking button | Chat shows `You don't have permission to open this menu!` (red); the sub-menu does NOT open and the PARENT menu stays open — the refusal is decided before the current inventory is closed. Then run the control, which must not depend on a live permission change reaching a player who is standing inside an open inventory: **close the menu** (chat is unavailable while an inventory is open, so this is needed simply to issue the next command), grant `test.submenu.gate`, **read the grant back with a command that resolves the node for the live player** — `/lp user <player> permission check test.submenu.gate`, or the installed plugin's equivalent — and only proceed once it answers true, then re-open the parent menu and click the same button — the sub-menu opens. The read-back is what makes this deterministic, and it has to ask the question the code asks: `MenuAccess#allowOpen` calls `player.hasPermission(...)` on the online player at click time, so a read-back that lists a user's STORED nodes (`permission info`) can show the node while the running player still resolves it false — which is the false `failed` this control exists to prevent. Re-opening the parent is not itself a flush; there is no per-menu permission cache to flush. That attributes the refusal to the key rather than to a button or menu that had stopped working. Being allowed into the parent grants nothing here: the key judged is the one on the menu being entered (`UltiKits/UltiMenu#15`) | java-client | CustomMenuGui |
| ultimenu.menu.submenu-open.neg-permission-unpaid | `language: en`; Vault and an economy provider installed; a test parent menu the clicking player is allowed to open, carrying a button with BOTH `price: 500` and `open-menu` naming a test sub-menu that sets `permission: "test.submenu.gate"`; the player holds `ultikits.menu.use` and at least 500 of currency but NOT `test.submenu.gate`; record his exact balance first, read from the economy provider itself rather than from any message | Click the button once, then click it twice more | The refusal appears each time (`You don't have permission to open this menu!`, red); the balance read from the provider is **unchanged after all three clicks**; no `Deducted …` line ever appears; the sub-menu never opens and the parent stays open. The three clicks matter: a charge that happened once could be mistaken for a rounding or display artefact, three identical clicks leaving the balance untouched cannot. Control, proving the fixture can actually charge: grant `test.submenu.gate`, read the grant back, re-open the parent, click once — the balance drops by exactly 500 and the sub-menu opens (`UltiKits/UltiMenu#20`) | server | CustomMenuGui |

## Configuration

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultimenu.config.config-yml | Fresh `config/config.yml` at its shipped default (`click_cooldown_ms: 200`) | Load the file; confirm the one key is present at its documented default; then set `click_cooldown_ms: 2000` (default 200) and run `/ul reload` (NOT `/menu reload` — `MenuCommands#onReload` only calls `MenuService#reload`, which re-parses `menus/*.yml` and never touches `MenuConfig`; only the framework-level reload, which runs this plugin's default `reloadSelf()` and reloads every `@ConfigEntity` including `MenuConfig`, actually picks up the new value), then repeat `ultimenu.menu.button-click.neg-debounce`'s test-menu double-click (the `close-on-click: false` button, not the shipped `example` menu's `spawn` button) but with a 500ms gap between clicks (inside the NEW 2000ms window, outside the OLD 200ms one) | The key is present at 200 before the change; after raising it to 2000 and running `/ul reload`, a second click 500ms after the first is ALSO suppressed — proving the raised value took effect, not merely surviving the old shorter window | server | |
| ultimenu.config.menu-definition-yml | `language: en`; sender holds `ultikits.menu.use` (needed for the `/menu list` and `/menu example` checks inside steps (a)/(b) below); fresh `menus/example.yml` at its shipped content — obtained by deleting the module's whole `menus/` folder (`plugins/UltiTools/pluginConfig/UltiTools-Menu/menus/`) and running `/menu reload` from the console, so `MenuServiceImpl#loadMenus` recreates it from this build (see Conventions: deleting only `example.yml` regenerates nothing, and a server first started on an earlier build still has the old file with `command: servermenu`) — (6 top-level keys — the shipped file no longer sets `command`, removed in 6.3.0, `UltiKits/UltiMenu#12`; 4 buttons, each carrying only the subset of the 11 possible button keys it actually sets — `info` sets 8 [`item`, `position`, `name`, `lore`, `player-commands`, `console-commands`, `price`, `close-on-click`]; `spawn` and `kit-starter` set 7 each [the same list minus `close-on-click`]; `rules` sets 7 [`item`, `position`, `name`, `lore`, `open-menu`, `price`, `close-on-click`]; NONE of the 4 shipped buttons sets `permission` or `custom-model-data` at all — both are exercised only via the parser's own default, not via this fixture) | Load the file; confirm all 6 top-level keys (`size`, `title`, `permission`, `bind-item`, `bind-name`, `bind-lore`) are present at their documented values and that the file has no top-level `command` line, and confirm each shipped button's OWN key subset (listed in this row's Preconditions) is present at its documented value — do NOT expect `permission` or `custom-model-data` to appear on any shipped button, their absence here is what exercises the parser's unset-key defaults (`ultimenu.config.menu-definition.buttons.permission`'s "no button-level check" and `.custom-model-data`'s "0 — none applied"), not a fixture gap; then exercise 3 representative changes in separate reloads so no change masks another's effect: (a) set `size: 10` (invalid — not a multiple of 9) and reload, confirming the menu fails to load entirely (a warning is logged, `/menu list` no longer shows `example`); restore `size: 27` before continuing; (b) set `permission: "test.menu.gate"` (default unset) and confirm a player holding `ultikits.menu.use` but lacking `test.menu.gate` is refused with `You don't have permission to open this menu!` on `/menu example` — granting the base command permission first ensures the refusal is the menu-specific half of `MenuAccess#allowOpen`, not its base-node half and not the class-level `ultikits.menu.use` gate rejecting the command before it runs at all — where an unset `permission` previously let the same player through; (c) run `/help menu` from the console and read the description registered for `/menu` at boot (`plugin.i18n(cmdExecutor.description())`, `UltiKits/UltiMenu#11`). The removed `command` key is exercised by `ultimenu.config.menu-definition-yml.neg-removed-command` below, not here | All 6 top-level keys, and no `command` line, and each shipped button's own key subset (8 for `info`, 7 each for `spawn`/`kit-starter`/`rules`), present at their documented values before any change, with `permission`/`custom-model-data` absent from every button and behaving per their stated defaults; (a) the invalid size fails the whole menu closed, not merely clamped; (b) the permission gate takes effect exactly as `ultimenu.config.menu-definition.permission` describes; (c) the registered command description reads `Menu management command`, with no Chinese text | server | |
| ultimenu.config.menu-definition-yml.neg-removed-command | `language: en` (both the `Reloaded 1 menus` reply and the warning line this row reads come from the language catalogue); `ultimenu.config.menu-definition-yml` has run, so `menus/example.yml` is the file this build regenerated, with no `command` line — if it has one, the fresh-file procedure was skipped: run it first rather than adding a second `command` line in step 1; `menus/example.yml` is the ONLY `.yml` file in the module's `menus/` folder (`plugins/UltiTools/pluginConfig/UltiTools-Menu/menus/`), so no other file can produce or suppress the line this row reads; the steps run from the console, which holds every permission `/menu reload` checks; the server console log is readable | 1. Add the top-level line `command: servermenu` to `menus/example.yml` directly below `title:` — exactly the line pre-6.3.0 builds shipped, and still present on any server first started on one — save, run `/menu reload`, and read the console. 2. Run `/menu reload` a second time without changing anything and read the console. 3. Delete the `command` line, save, run `/menu reload` again, and read the console | Step 1 prints exactly one line containing `UltiMenu: 'command' in `, followed by a path ending in `menus/example.yml`, then `no longer has any effect and can be deleted from the file`; the same line also contains `UltiKits/UltiMenu#23` and `UltiKits/UltiMenu#12`. The reload itself still succeeds — `Reloaded 1 menus` — so the key is reported, not refused. Step 2 prints that same line again (it is reported at every load, not once). Step 3 prints no line containing `'command' in` and still `Reloaded 1 menus`. Step 1 is this row's positive control and must be read first: without it, step 3's silence is indistinguishable from a check that never fires (`UltiKits/UltiMenu#12`) | server | |

## Language

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultimenu.i18n.language | `language: en` in the framework's `plugins/UltiTools/config.yml`, set once for the session before the start; before the start, `plugins/UltiTools/pluginConfig/UltiTools-Menu/lang/` was moved into the session backup directory so this jar's catalogues are extracted fresh (an upgraded install never refreshes an already-extracted language file, UltiKits/UltiTools-Reborn#459) | From the server console run `/menu help` | The console shows these five lines, and none of the lines this command printed contains Chinese text: `=== UltiMenu Command Help ===`, `/menu <name> - Open the named menu`, `/menu open <name> - Open the named menu (explicit form)`, `/menu list - List all available menus`, `/menu reload - Reload the menu configuration (requires admin permission)` (colour codes rendered or stripped by the console, text otherwise exact) | server | |
